#ifndef GUARD_fail_h
#define GUARD_fail_h

// fail.h
#include <list>
#include <vector>
#include "Student_info.h"
#include "grade.h"

bool fgrade(const Student_info&);
students_type extract_fails_v1(students_type&);
students_type extract_fails_v2(students_type&);

#endif