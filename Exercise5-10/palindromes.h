#ifndef GUARD_palindromes_h
#define GUARD_palindromes_h

#include <string>
#include <list>

using std::string;
using std::list;

bool palindromes(string&);
list<string> extract_palindromes(list<string>&);

#endif